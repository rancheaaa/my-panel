# 测试报告（my-panel-admin / my-panel-auth-lite）

## 1. 单元测试（my-panel-auth-lite）

- AuthFilter：覆盖 OPTIONS 放行、白名单放行、缺失/非 Bearer/空 token/无效 token 的 401、有效 token 设置上下文并在链路结束清理上下文
  - [AuthFilterTest.java](file:///e:/java-project2/my-panel/my-panel-auth-lite/src/test/java/com/cq/panel/authlite/filter/AuthFilterTest.java)
- AuthorizationService：覆盖无用户/有用户时的角色与权限判断
  - [AuthorizationServiceTest.java](file:///e:/java-project2/my-panel/my-panel-auth-lite/src/test/java/com/cq/panel/authlite/AuthorizationServiceTest.java)
- AOP 注解拦截（@RequireRole/@RequirePermission）：覆盖无注解不拦截、无用户/无权限抛 403、授权通过、JDK 代理场景下从目标方法解析注解
  - [AuthorizationAdvisorsTest.java](file:///e:/java-project2/my-panel/my-panel-auth-lite/src/test/java/com/cq/panel/authlite/aop/AuthorizationAdvisorsTest.java)
- 性能测试（单线程循环鉴权）：以简化版 Spring Security 核心模型（ThreadLocal + AuthenticationToken + principal）为基线，10000 次以上循环，耗时不超过 120%
  - [AuthorizationPerformanceTest.java](file:///e:/java-project2/my-panel/my-panel-auth-lite/src/test/java/com/cq/panel/authlite/AuthorizationPerformanceTest.java)

执行命令与结果：

- `mvn -pl my-panel-auth-lite test`：BUILD SUCCESS

## 2. 集成测试（my-panel-admin）

- MockMvc 回归（不少于 30 个 GET 鉴权接口）：
  - 未登录（无 Authorization 头）返回 401
  - 无权限（Bearer noperm）返回 403
  - 已登录且具备权限（Bearer admin）返回 200
  - 测试用例：[AuthLiteMockMvcRegressionTest.java](file:///e:/java-project2/my-panel/my-panel-admin/src/test/java/com/cq/panel/admin/server/security/AuthLiteMockMvcRegressionTest.java)

执行命令与结果：

- `mvn -pl my-panel-admin -am test`：BUILD SUCCESS

