# 现有鉴权点清单（my-panel-admin）

## 0. Spring Security API 直接引用点（扫描结果）

- Java 源码中 `org.springframework.security.*`、`@PreAuthorize`、`@Secured`、`SecurityContextHolder`、`antMatchers/mvcMatchers/regexMatchers`：0 处
- 仅在迁移脚本与迁移文档中保留了旧写法字符串/说明

## 1. URL 级规则（当前鉴权框架）

来源：[SecurityConfig.java](file:///e:/java-project2/my-panel/my-panel-admin/src/main/java/com/cq/panel/admin/server/config/SecurityConfig.java#L96-L136)

### 允许匿名访问（白名单）
- 动态放行：`permitAllUrl.getUrls()`（由 `@Anonymous` 扫描采集）
  - 采集逻辑：[PermitAllUrlProperties.java](file:///e:/java-project2/my-panel/my-panel-admin/src/main/java/com/cq/panel/admin/server/common/properties/PermitAllUrlProperties.java#L38-L55)
- 静态放行：
  - `/login`、`/register`、`/captchaImage`
  - `/logout`
  - `/actuator/**`
  - `/admin/server/**`
  - `/agent/registry/register`
  - `/agent/registry/heartbeat`
  - `GET` 静态资源：`/`、`/*.html`、`/**.html`、`/**.css`、`/**.js`、`/profile/**`
  - Swagger/Doc/Druid：`/swagger-ui.html`、`/v3/api-docs/**`、`/swagger-ui/**`、`/druid/**`、`/doc.html`

### 其余路径
- 由 `AuthFilter` 进行登录态校验（未登录返回 401）

## 2. 方法级鉴权点（@RequirePermission/@RequireRole）

- 方法级鉴权入口已迁移为 `@RequirePermission`（权限点字符串），未发现 `@RequireRole` 实际使用点（0 处）。
- URL 与权限点对应关系见：`docs/security-migration/url-permission.csv`
