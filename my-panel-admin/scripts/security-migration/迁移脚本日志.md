# 迁移脚本执行日志（security-migration）

## 1. 注解迁移

- 脚本： [replace_security_annotations.py](file:///e:/java-project2/my-panel/my-panel-admin/scripts/security-migration/replace_security_annotations.py)
- 目标：
  - `@PreAuthorize("@ss.hasPermi('X')")` -> `@RequirePermission("X")`
  - `@Secured("ROLE")` -> `@RequireRole("ROLE")`（当前代码未发现实际使用点）

## 2. 扫描结果（当前仓库状态）

- my-panel-admin Java 源码中：
  - `@PreAuthorize`：0 处
  - `@Secured`：0 处
  - `SecurityContextHolder`：0 处
  - `org.springframework.security.*`：0 处

## 3. 备注

- 保留了旧写法的字符串/示例仅用于迁移脚本与迁移文档说明，不影响编译与运行。

