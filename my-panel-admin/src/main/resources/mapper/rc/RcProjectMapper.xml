<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cq.panel.admin.server.repository.mapper.RcProjectMapper">
    
    <resultMap type="RcProject" id="RcProjectResult">
        <result property="id"    column="id"    />
        <result property="projectName"    column="project_name"    />
        <result property="projectDesc"    column="project_desc"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectRcProjectVo">
        select id, project_name, project_desc, create_by, create_time, update_by, update_time from rc_project
    </sql>

    <select id="selectRcProjectList" parameterType="RcProject" resultMap="RcProjectResult">
        <include refid="selectRcProjectVo"/>
        <where>  
            <if test="projectName != null  and projectName != ''"> and project_name like concat('%', #{projectName}, '%')</if>
        </where>
    </select>
    
    <select id="selectRcProjectById" parameterType="Long" resultMap="RcProjectResult">
        <include refid="selectRcProjectVo"/>
        where id = #{id}
    </select>

    <select id="checkProjectNameUnique" parameterType="String" resultMap="RcProjectResult">
        <include refid="selectRcProjectVo"/>
        where project_name = #{projectName} limit 1
    </select>
        
    <insert id="insertRcProject" parameterType="RcProject" useGeneratedKeys="true" keyProperty="id">
        insert into rc_project
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="projectName != null and projectName != ''">project_name,</if>
            <if test="projectDesc != null">project_desc,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="projectName != null and projectName != ''">#{projectName},</if>
            <if test="projectDesc != null">#{projectDesc},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateRcProject" parameterType="RcProject">
        update rc_project
        <trim prefix="SET" suffixOverrides=",">
            <if test="projectName != null and projectName != ''">project_name = #{projectName},</if>
            <if test="projectDesc != null">project_desc = #{projectDesc},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteRcProjectById" parameterType="Long">
        delete from rc_project where id = #{id}
    </delete>

    <delete id="deleteRcProjectByIds" parameterType="String">
        delete from rc_project where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
