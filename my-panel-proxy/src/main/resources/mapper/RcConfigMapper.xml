<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cq.proxy.repository.mapper.RcConfigMapper">

    <select id="selectByEnvProjectKey" resultType="com.cq.proxy.repository.entity.RcConfig">
        SELECT * FROM rc_config
        WHERE env_id = #{envId}
          AND project_id = #{projectId}
          AND config_key = #{configKey}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.cq.proxy.repository.entity.RcConfig" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO rc_config (env_id, project_id, config_key, config_value, config_desc, source, create_time, update_time)
        VALUES (#{envId}, #{projectId}, #{configKey}, #{configValue}, #{configDesc}, #{source}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById" parameterType="com.cq.proxy.repository.entity.RcConfig">
        UPDATE rc_config
        SET config_value = #{configValue},
            config_desc = #{configDesc},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

</mapper>
