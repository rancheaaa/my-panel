<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cq.proxy.repository.mapper.RcNodeMapper">

    <select id="selectByEnvProjectIpPort" resultType="com.cq.proxy.repository.entity.RcNode">
        SELECT * FROM rc_node
        WHERE env_id = #{envId}
          AND project_id = #{projectId}
          AND node_ip = #{nodeIp}
          AND node_port = #{nodePort}
    </select>

    <select id="selectByEnvProjectStatus" resultType="com.cq.proxy.repository.entity.RcNode">
        SELECT * FROM rc_node
        WHERE env_id = #{envId}
          AND project_id = #{projectId}
          AND status = #{status}
    </select>

    <select id="selectByStatus" resultType="com.cq.proxy.repository.entity.RcNode">
        SELECT * FROM rc_node
        WHERE status = #{status}
    </select>

    <insert id="insert" parameterType="com.cq.proxy.repository.entity.RcNode" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO rc_node (env_id, project_id, node_ip, node_port, status, zone, last_refresh_time, create_time, update_time)
        VALUES (#{envId}, #{projectId}, #{nodeIp}, #{nodePort}, #{status}, #{zone}, #{lastRefreshTime}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById" parameterType="com.cq.proxy.repository.entity.RcNode">
        UPDATE rc_node
        SET status = #{status},
            zone = #{zone},
            last_refresh_time = #{lastRefreshTime},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

</mapper>
